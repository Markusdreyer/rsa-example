public class CalculatePhiTest {
    public static void main(String[] args) {
        int p = 2;
        int q = 10;
        int n = p * q;
        int phiOfN = (p - 1) * (q - 1);
        System.out.println("Phi of N: " + phiOfN + "\nN: " + n);
    }

    /*
    TODO: Fix Euliers Totient
     */
}
